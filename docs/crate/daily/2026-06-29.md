# Crate Daily Note - 2026-06-29

## Active Version

- Engineering baseline before redesign implementation: `v2.8.0-qa.41`
- Canonical branch: `v2.4.x`
- Current phase: post-redesign QA preparation

## Redesign Buildout Status

Bryant reported that Jenna finished the new Crate macOS app buildout in Figma.

Current context:

- Bryant downloaded Figma and logged into Jenna's account.
- The Figma plugin was used to create the next Crate build for QA.
- Bryant plans to run smoke tests before preparing the app for real testers.

Implementation update:

- Branch: `codex/redesign-current-model-ui`
- Scope: renderer-only current-model UI merge
- Changed app files:
  - `renderer/index.html`
  - `renderer/app.js`
  - `renderer/styles.css`
- The implementation keeps the existing Crate Electron architecture and package engine.
- The renderer now uses the simplified nav model:
  - `Projects`
  - `Current Project`
  - `Settings`
  - `Help`
- Quick Package remains a prominent action, not a primary nav tab.
- The renderer no longer exposes a Home tab or top-level Files tab.
- Direct Figma plugin tools were unavailable in this thread, so the implementation used the active Figma appshot/frame inventory plus the redesign docs and Bryant's explicit instructions.

## Product / Release Guardrail

This is not final public release prep yet.

Before real testers:

- Review the generated app changes from the Figma-plugin build.
- Confirm the app still follows the simplified current-model navigation:
  - `Projects`
  - `Current Project`
  - `Settings`
  - `Help`
  - Quick Package as a prominent action
- Confirm no `Home` dashboard returned.
- Confirm no top-level `Files` tab returned.
- Confirm no Wispr/productivity behavior returned.
- Confirm qa41 packaging behavior is preserved.
- Prepare and run a post-redesign internal QA build, likely `v2.8.0-qa.42`.

## Next Action

Review the local implementation on `codex/redesign-current-model-ui`.

Then run a design-to-build review before any tester distribution:

- inspect changed app files
- verify no unintended backend/package logic changes
- run focused renderer checks
- prepare `qa.42` internal QA gate if the build is clean
- run post-redesign smoke tests before real tester outreach
