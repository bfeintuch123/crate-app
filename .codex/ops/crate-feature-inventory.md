# Crate Feature Inventory

Generated from the canonical JSON catalog. Automated evidence is verified by named assertion markers, but passing results still come from the relevant check suite.
Supported-app claims remain owned by `crate-supported-app-claims`; step-by-step QA procedures remain owned by `crate-manual-qa-matrix`.

| Feature | User story | Source evidence | Automated evidence | Coverage note | QA lanes |
| --- | --- | --- | --- | --- | --- |
| App window lifecycle (`app-window-lifecycle`) | As a user, I can launch, reopen, and activate Crate without a zero-window state. | `main.js` | `tests/main-window-lifecycle.test.js` (`appHandlers.get('activate')`) | Automated evidence covers the stated story; manual QA remains release evidence. | clean launch, force quit/reopen, Dock activation |
| Projects and Current Project (`projects-current-project`) | As a designer, I can create, select, watch, pause, and return to a project workspace. | `main.js`, `renderer/app.js`, `renderer/index.html` | `tests/renderer-figma-scope.test.js` (`btn-start-project`) | Automation covers opening the project form. Selection, watch, pause, and return remain installed-app QA lanes. | Projects, Current Project, watch/pause/resume |
| Dependency capture (`dependency-capture`) | As a designer, Crate gathers project dependencies from supported creative applications while I work. | `main.js`, `parsers/aftereffects.js`, `parsers/ai.js`, `parsers/base.js` | `tests/provenance.test.js` (`APP_OPENED_FILE`) | Automation covers provenance primitives only. Multi-application live capture remains parser tests plus manual creative-app QA. | Illustrator, InDesign, Photoshop, PowerPoint, Keynote |
| Quick Package (`quick-package`) | As a user, I can package one approved source file without starting a watched project. | `main.js`, `parsers/index.js`, `renderer/app.js` | `tests/quick-package-parser.test.js` (`Quick Package extracts PowerPoint embedded media without reporting them missing`) | Automation covers parser output. The Quick Package UI, destination choice, and quota behavior have separate evidence lanes. | Quick Package, quota increment, output scope |
| Package review, complete, and details (`package-review-details`) | As a user, I can review package contents and understand what Crate included or excluded. | `main.js`, `renderer/app.js`, `renderer/index.html` | `tests/renderer-figma-scope.test.js` (`Package Details shows the no-issue state`) | Automation covers Package Details rendering. Package Review and Package Complete remain installed-app QA lanes. | Package Review, Package Complete, Package Details |
| Needs save (`needs-save`) | As a user, I am warned when an unsaved source document prevents safe linked-asset packaging. | `main.js`, `renderer/app.js` | Manual-only: The Illustrator unsaved-linked-asset lane is validated through Illustrator Smoke 2. | Manual-only coverage is explicit; add automation when a stable harness exists. | Illustrator Smoke 2, files waiting for review |
| Figma project link and scope (`figma-project-link`) | As a Figma user, I can link a project with Current Page Only by default and opt into Entire File. | `main.js`, `parsers/figma.js`, `preload.js`, `renderer/app.js` | `tests/figma-link-per-project.test.js` (`explicit Entire File still packages Figma assets from any page`); `tests/renderer-figma-scope.test.js` (`defaults missing or invalid scope to Current Page Only`) | Automated evidence covers the stated story; manual QA remains release evidence. | Figma connected, Current Page Only, Entire File, fail closed |
| Figma privacy and errors (`figma-privacy-errors`) | As a user, Figma errors remain actionable without exposing tokens, file keys, URLs, or signed assets. | `main.js`, `parsers/figma.js`, `renderer/app.js` | `tests/figma-token-privacy.test.js` (`assertNoFigmaSecrets`); `tests/figma-scope.test.js` (`fails closed with a safe diagnostic`) | Automated evidence covers the stated story; manual QA remains release evidence. | disconnected, reconnect, rate limited, cannot read |
| Diagnostics and provenance (`diagnostics-provenance`) | As a user, diagnostics are off by default and remain isolated from normal design assets when enabled. | `main.js`, `provenance.js`, `renderer/app.js` | `tests/psd-embedded-safety.test.js` (`default package omits diagnostic report files`); `tests/psd-embedded-safety.test.js` (`package provenance records copied files and skips missing files`) | Automation covers default-off and enabled manifest placement; installed-app QA confirms the Settings toggle and Package Details copy. | Diagnostics OFF, Diagnostics ON, privacy scan |
| Package exclusions (`package-exclusions`) | As a user, Crate excludes package outputs, old QA roots, diagnostics, and unrelated local files. | `main.js`, `parsers/package-safety.js`, `provenance.js` | `tests/provenance-dual-write.test.js` (`automatic live capture ignores old package output and diagnostics folders`); `tests/psd-embedded-safety.test.js` (`Package Copy Containment`) | Automated evidence covers the stated story; manual QA remains release evidence. | output recapture, unrelated files, old QA roots |
| Quota and plan states (`quota-plan`) | As a user, successful packages increment quota and exhausted quota writes no output. | `main.js`, `renderer/app.js`, `renderer/index.html` | `tests/provenance-dual-write.test.js` (`Quick Package consumes package quota only after successful packaging`) | Automated evidence covers the stated story; manual QA remains release evidence. | quota increment, limit reached, no output written |
| Idle project alerts (`idle-alerts`) | As a user, I can keep watching, pause, or package a project from the inactivity alert. | `main.js`, `renderer/app.js` | Manual-only: Keep Watching, Pause, and Package Now are validated in the installed-app idle-alert smoke lane. | Manual-only coverage is explicit; add automation when a stable harness exists. | Keep Watching, Pause, Package Now |
